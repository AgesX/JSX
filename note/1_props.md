<Counter
            key={countPiece.key}
            value={countPiece.val}
            selected={true}
          />

Counter 组件的 props ，

props 的属性中，不包含 key,

key is a special attribute
for uniquely identifiying elements
